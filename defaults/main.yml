# Standards: 0.1
---
# @var google_chrome_user:description: User to run user-specific commands
# @var google_chrome_user:value: google_chrome_user | default(homebrew_user) | default(ansible_user_id)
# google_chrome_user:

# @var google_chrome_started:description: Restart app if already running
google_chrome_started: True

...
